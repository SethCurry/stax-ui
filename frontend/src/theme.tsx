import { createTheme } from "@mui/material/styles";

export const defaultTheme = createTheme({
  palette: {
    primary: {
      light: "#DBC2FF",
      main: "#7900ff",
      dark: "#0000E8",
      contrastText: "#84FF00",
    },
  },
});
